use Test;
use OO::Monitors;

plan 1;

monitor Grid {
    has @.grid;

    submethod BUILD() {
        @!grid = [23, 42];
    }
}

my $g = Grid.new;
is $g.grid, [23, 42], 'BUILD submethod ran OK and set attribute';

# vim: expandtab shiftwidth=4
